<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dndapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme">

        <activity android:name=".CharacterView"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ViewSpellCard"
            android:theme="@style/Theme.AppCompat">
        </activity>
        <activity
            android:name=".ViewFilter"
            android:theme="@style/Theme.AppCompat">
        </activity>
        <activity android:name=".CharacterCreation"
            android:theme="@style/Theme.AppCompat">
        </activity>
        <!--<activity-->
            <!--android:name=".CharacterView">-->
        <!--</activity>-->
        <activity
            android:name=".SpellCardGallery"
            android:theme="@style/Theme.AppCompat">
        </activity>
        <activity
            android:name=".CharacterProfile"
            android:theme="@style/Theme.AppCompat">
        </activity>
    </application>

</manifest>